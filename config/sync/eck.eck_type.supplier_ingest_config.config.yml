uuid: 6d475766-8bfb-4ab2-a4b0-72fed97ad2e1
langcode: en
status: true
dependencies:
  config:
    - eck.eck_entity_type.supplier_ingest_config
name: Config
type: config
description: 'Per-supplier ingest configuration: column mapping, default cost UOM, bundle policy (matched-only / discovery / both / excluded per material bundle), and fuzzy-match thresholds. One config per supplier (uniqueness enforced in 3.2 hook).'
