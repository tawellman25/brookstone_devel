uuid: 91d7428a-8ef1-410a-8d3d-41c511faff04
langcode: en
status: true
dependencies:
  config:
    - eck.eck_type.time_clock_entry.entry
    - field.storage.time_clock_entry.field_external_punch_id
id: time_clock_entry.entry.field_external_punch_id
field_name: field_external_punch_id
entity_type: time_clock_entry
bundle: entry
label: 'External Punch ID'
description: 'Unique identifier from the external time clock system (e.g., the TimeTrax punch primary key). Used to prevent duplicate imports when the same punch event is processed more than once. Leave blank for manually-entered records. <strong>MUST be unique across all time_clock_entry records when populated</strong> — uniqueness is enforced by import modules at write time, not by Drupal field config.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
