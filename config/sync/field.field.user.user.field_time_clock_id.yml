uuid: a7a266fe-1016-4b2a-a0c8-e42e91259ec7
langcode: en
status: true
dependencies:
  config:
    - field.storage.user.field_time_clock_id
  module:
    - user
id: user.user.field_time_clock_id
field_name: field_time_clock_id
entity_type: user
bundle: user
label: 'Time Clock ID'
description: 'Employee ID used by the external time clock system (e.g., the card number, badge ID, or employee number assigned in Timetrax). Used to map imported time clock records to this BOS user. <strong>MUST be unique across users when populated</strong> — duplicates will cause incorrect attribution of imported time records.'
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
