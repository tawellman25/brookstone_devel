langcode: en
status: true
dependencies:
  config:
    - eck.eck_type.wo_material_list_item.items
    - field.storage.wo_material_list_item.field_supplier_item_number
id: wo_material_list_item.items.field_supplier_item_number
field_name: field_supplier_item_number
entity_type: wo_material_list_item
bundle: items
label: 'Supplier Item #'
description: "The vendor's SKU / item number for the material you bought. Helpful so the office can re-order the same item from the same vendor in the future. Auto-normalized on save (common pasted prefixes like 'Item #' / 'SKU' are stripped). Only synced into the supplier catalog when the catalog entry doesn't already have a SKU — manual edits to the supplier catalog are never overwritten."
required: false
translatable: false
default_value: {  }
default_value_callback: ''
settings: {  }
field_type: string
