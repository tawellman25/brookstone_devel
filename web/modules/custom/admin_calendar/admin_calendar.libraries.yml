fullcalendar:
  js:
    https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js:
      type: external
      minified: true
  # FullCalendar v6 injects CSS via JS — no separate CSS file needed.

admin_calendar:
  js:
    js/admin_calendar.js: {}
  css:
    theme:
      css/admin_calendar.css: {}
  dependencies:
    - admin_calendar/fullcalendar
    - core/drupal
    - core/drupalSettings
