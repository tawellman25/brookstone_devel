name: 'BOS Backflow Device Types'
type: module
description: 'Audience view-mode tiers (office/admin → admin_view, crew → teammate_view, public → full) + a rotating full-bleed hero banner (field_banner_images) for the backflow_device_types vocabulary. Mirrors bos_services.'
package: 'BOS'
core_version_requirement: ^10 || ^11
dependencies:
  - drupal:taxonomy
