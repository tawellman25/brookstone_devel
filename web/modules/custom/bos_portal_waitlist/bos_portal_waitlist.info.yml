name: BOS Portal Waitlist
type: module
description: 'Customer-portal waitlist panel on the login page. Captures interest + opt-ins onto Contact (consent home), never creates accounts. Product signal kept out of the consent log.'
package: BOS
core_version_requirement: ^10 || ^11
dependencies:
  - eck:eck
  - captcha:captcha
  - bos_consent_log:bos_consent_log
