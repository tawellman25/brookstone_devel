winterize:
  css:
    theme:
      # Oswald (display + accordion summaries). External; a blocked request just
      # falls back to the stack in bo-winterize.css — the page still works.
      https://fonts.googleapis.com/css2?family=Oswald:wght@500;600;700&display=swap: { type: external, minified: true }
      css/bo-tokens.css: {}
      css/bo-winterize.css: {}
