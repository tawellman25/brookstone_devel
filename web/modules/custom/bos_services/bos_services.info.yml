name: 'BOS Services'
type: module
description: 'Role-based rendering for the services taxonomy: public "what we do" (full) vs teammate "how we do it" training (teammate_view) on the same /services URL.'
package: BOS
core_version_requirement: ^10 || ^11
