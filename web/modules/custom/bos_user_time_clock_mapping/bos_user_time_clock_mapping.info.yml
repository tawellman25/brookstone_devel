name: 'BOS User Time Clock Mapping'
type: module
description: 'Hides the External Time Clock Mapping fieldset on the user edit form when the user being edited does not have the teammates role. Non-teammates (clients, public users) never need a time clock mapping.'
core_version_requirement: ^10
package: BOS
dependencies:
  - drupal:user
