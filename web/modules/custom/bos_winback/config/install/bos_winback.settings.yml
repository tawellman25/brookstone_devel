# Win-back campaign code prefix. The current two-digit year is appended at
# booking time (react + 26 = react26), stored on the record, never recomputed.
# Change with: drush cset bos_winback.settings campaign_prefix <prefix>
campaign_prefix: react
