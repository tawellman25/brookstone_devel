name: 'BOS WO Intake (Cowork Connect)'
type: module
description: 'Authenticated WO-intake REST endpoint for the Cowork integration. Custom X-API-KEY auth over a transport-agnostic WorkOrderIntakeService. Gate 1: bare WO creation from IDs.'
package: 'Integration'
core_version_requirement: ^10 || ^11
dependencies:
  - drupal:rest
  - drupal:serialization
  - key:key
  - eck:eck
