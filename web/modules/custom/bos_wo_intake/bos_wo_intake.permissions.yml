use work order intake:
  title: 'Use the Work Order intake page'
  description: 'Access the /wo-intake voice/text page to create Work Orders from a spoken or typed command. Note: the user also needs the entity create perms (create work_order entities, create wo_notes entities of bundle note) for a WO to actually be created — the page gate and the entity gate are separate.'
  restrict access: true
