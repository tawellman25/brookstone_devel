<?php

declare(strict_types=1);

use Drupal\Core\Entity\EntityInterface;
use Drupal\contract_residential\ContractActionLogWriter;
use Symfony\Component\Validator\ConstraintViolation;
use Symfony\Component\Validator\ConstraintViolationListInterface;

/**
 * Implements hook_entity_view_mode_alter().
 */
function contract_residential_entity_view_mode_alter(&$view_mode, EntityInterface $entity, $context) {
  if ($entity->getEntityTypeId() === 'contracts' && $entity->bundle() === 'residential') {
    $user = \Drupal::currentUser();
    if ($user->hasRole('site_admin') || $user->hasRole('administration') || $user->hasRole('administrator')) {
      $view_mode = 'admin';
    }
  }
}

/**
 * Implements hook_entity_validate().
 *
 * Friendly validation (no WSOD):
 * - Enforce one Residential Contract per Property per Year.
 * - Enforce Contract Year format.
 */
function contract_residential_entity_validate(EntityInterface $entity, ConstraintViolationListInterface $violations) {
  if ($entity->getEntityTypeId() !== 'contracts' || $entity->bundle() !== 'residential') {
    return;
  }

  $property_field = 'field_property';
  $year_field = 'field_contract_year';

  if (!$entity->hasField($property_field) || $entity->get($property_field)->isEmpty()) {
    return;
  }
  $property_id = (int) $entity->get($property_field)->target_id;
  if ($property_id <= 0) {
    return;
  }

  $year = '';
  if ($entity->hasField($year_field) && !$entity->get($year_field)->isEmpty()) {
    $year = trim((string) $entity->get($year_field)->value);
  }
  $year = substr($year, 0, 4);

  if ($year === '' || !preg_match('/^\d{4}$/', $year)) {
    $violations->add(new ConstraintViolation(
      'Contract Year must be a 4-digit year like 2026.',
      'Contract Year must be a 4-digit year like 2026.',
      [],
      $entity,
      $year_field,
      $year
    ));
    return;
  }

  $query = \Drupal::entityQuery('contracts')
    ->accessCheck(FALSE)
    ->condition('type', 'residential')
    ->condition($property_field, $property_id)
    ->condition($year_field, $year);

  if (!$entity->isNew()) {
    $query->condition('id', (int) $entity->id(), '<>');
  }

  $existing = $query->range(0, 1)->execute();
  if (!empty($existing)) {
    $existing_id = (int) array_key_first($existing);

    $violations->add(new ConstraintViolation(
      'A Residential Contract already exists for this Property for @year (Contract ID: @id). Update the existing Contract instead.',
      'A Residential Contract already exists for this Property for @year (Contract ID: @id). Update the existing Contract instead.',
      ['@year' => $year, '@id' => $existing_id],
      $entity,
      $property_field,
      $property_id
    ));
  }
}

/**
 * Implements hook_entity_presave().
 */
function contract_residential_entity_presave(EntityInterface $entity) {
  if ($entity->getEntityTypeId() !== 'contracts' || $entity->bundle() !== 'residential') {
    return;
  }

  $year_field = 'field_contract_year';

  $year = '';
  if ($entity->hasField($year_field) && !$entity->get($year_field)->isEmpty()) {
    $year = trim((string) $entity->get($year_field)->value);
  }

  if ($year === '') {
    $created_ts = NULL;

    if (method_exists($entity, 'getCreatedTime')) {
      $created_ts = (int) $entity->getCreatedTime();
    }
    elseif ($entity->hasField('created') && !$entity->get('created')->isEmpty()) {
      $created_ts = (int) $entity->get('created')->value;
    }

    if (!empty($created_ts) && $entity->hasField($year_field)) {
      $entity->set($year_field, (string) date('Y', $created_ts));
    }
  }
}

/**
 * Implements hook_entity_insert().
 */
function contract_residential_entity_insert(EntityInterface $entity) {

  if ($entity->getEntityTypeId() === 'contracts' && $entity->bundle() === 'residential') {
    _contract_residential_sync_section_parent_links($entity);
    return;
  }

  if ($entity->getEntityTypeId() === 'contract_sections') {
    _contract_residential_stamp_parent_contract_last_updated($entity);
    _contract_residential_maybe_sync_weed_spray_route($entity);
  }
}

/**
 * Implements hook_entity_update().
 */
function contract_residential_entity_update(EntityInterface $entity) {

  if ($entity->getEntityTypeId() === 'contracts' && $entity->bundle() === 'residential') {
    _contract_residential_sync_section_parent_links($entity);
    _contract_residential_maybe_generate_work_orders_on_status($entity);
    return;
  }

  if ($entity->getEntityTypeId() === 'contract_sections') {
    _contract_residential_stamp_parent_contract_last_updated($entity);
    _contract_residential_maybe_sync_weed_spray_route($entity);
  }
}

/**
 * Planned feature: auto-generate work orders when contract status changes.
 *
 * Not yet implemented. Stub exists to prevent fatal error from the call
 * in contract_residential_entity_update().
 */
function _contract_residential_maybe_generate_work_orders_on_status(EntityInterface $entity): void {
  // @todo Implement work order auto-generation on contract status change.
}

/**
 * Stamp parent Contract when a Contract Section is saved.
 */
function _contract_residential_stamp_parent_contract_last_updated(EntityInterface $section): void {
  static $in_progress = FALSE;
  if ($in_progress) {
    return;
  }

  if (!$section->hasField('field_contract') || $section->get('field_contract')->isEmpty()) {
    return;
  }

  $contract_id = (int) $section->get('field_contract')->target_id;
  if ($contract_id <= 0) {
    return;
  }

  $contract_storage = \Drupal::entityTypeManager()->getStorage('contracts');
  $contract = $contract_storage->load($contract_id);
  if (!$contract) {
    return;
  }

  if (!$contract->hasField('field_last_updated_at') || !$contract->hasField('field_last_updated_by')) {
    return;
  }

  $now = (int) \Drupal::time()->getRequestTime();
  $uid = (int) \Drupal::currentUser()->id();

  $contract->set('field_last_updated_at', $now);
  $contract->set('field_last_updated_by', $uid);

  $in_progress = TRUE;
  try {
    $contract->save();
  }
  finally {
    $in_progress = FALSE;
  }
}

/**
 * Implements hook_cron().
 */
function contract_residential_cron(): void {
  _contract_residential_checkups_enqueue_dispatch(FALSE, 'cron');
}

/**
 * Enqueue the checkup dispatch item, guarded to run at most once per day.
 *
 * Pushes one 'dispatch' item onto the contract_residential_checkup_generator
 * queue. The queue worker (ContractResidentialCheckupGeneratorQueueWorker)
 * fans that out into individual per-section 'process_section' items on the
 * same queue.
 *
 * A daily guard via Drupal State prevents duplicate dispatches when cron
 * runs more than once in a day. Pass $force = TRUE to bypass the guard
 * (e.g. drush bos:checkups:generate --force).
 *
 * State key:  contract_residential.checkups_last_dispatch_date
 * Queue name: contract_residential_checkup_generator
 *
 * @param bool $force
 *   Skip the daily guard and enqueue regardless of last dispatch date.
 * @param string $source
 *   Caller identifier included in the queue item and the log entry
 *   ('cron', 'drush').
 */
function _contract_residential_checkups_enqueue_dispatch(bool $force, string $source): bool {
  $state_key = 'contract_residential.checkups_last_dispatch_date';
  $queue_name = 'contract_residential_checkup_generator';
  $today = date('Y-m-d');

  if (!$force) {
    $last_dispatch = (string) \Drupal::state()->get($state_key, '');
    if ($last_dispatch === $today) {
      return FALSE;
    }
  }

  \Drupal::queue($queue_name)->createItem([
    'op' => 'dispatch',
    'queued_at' => \Drupal::time()->getRequestTime(),
    'source' => $source,
  ]);

  \Drupal::state()->set($state_key, $today);

  \Drupal::logger('contract_residential_checkups')->info(
    'Checkup dispatch enqueued. Source: @source. Force: @force. Date: @date. Queue: @queue.',
    [
      '@source' => $source,
      '@force' => $force ? 'yes' : 'no',
      '@date' => $today,
      '@queue' => $queue_name,
    ]
  );

  return TRUE;
}

/**
 * Sync field_contract back-reference on every referenced contract section.
 *
 * When a residential contract is saved, iterate over all section reference
 * fields and ensure each referenced contract_sections entity has its
 * field_contract pointing back to this contract.
 */
function _contract_residential_sync_section_parent_links(EntityInterface $entity): void {
  $field_definitions = \Drupal::service('entity_field.manager')
    ->getFieldDefinitions('contracts', 'residential');

  $contract_id = (int) $entity->id();
  $section_storage = \Drupal::entityTypeManager()->getStorage('contract_sections');

  foreach ($field_definitions as $field_name => $definition) {
    if ($definition->getType() !== 'entity_reference') {
      continue;
    }
    $settings = $definition->getSettings();
    if (empty($settings['target_type']) || $settings['target_type'] !== 'contract_sections') {
      continue;
    }
    if ($entity->get($field_name)->isEmpty()) {
      continue;
    }

    foreach ($entity->get($field_name) as $item) {
      $section_id = $item->target_id;
      if (empty($section_id)) {
        continue;
      }
      $section = $section_storage->load($section_id);
      if (!$section) {
        continue;
      }
      if (!$section->hasField('field_contract')) {
        continue;
      }
      $current_ref = $section->get('field_contract')->target_id;
      if ((int) $current_ref === $contract_id) {
        continue;
      }
      $section->set('field_contract', $contract_id);
      $section->save();
    }
  }
}

/**
 * Syncs property_spraying_info:weed_spraying when weed spray contract sections
 * are saved.
 *
 * Replicates the logic from the SprayRouteOn/Off VBO actions but fires
 * automatically on section save. Only processes 2026+ contracts.
 */
function _contract_residential_maybe_sync_weed_spray_route(EntityInterface $entity): void {
  if ($entity->getEntityTypeId() !== 'contract_sections') {
    return;
  }
  $bundle = $entity->bundle();
  if (!in_array($bundle, [
    'weed_spraying_landscape_beds',
    'weed_spraying_of_misc_areas',
  ], TRUE)) {
    return;
  }

  if ($entity->get('field_do_you_want')->isEmpty()) {
    return;
  }
  $do_you_want = (string) $entity->get('field_do_you_want')->value;

  // Load parent contract via field_contract.
  if ($entity->get('field_contract')->isEmpty()) {
    return;
  }
  $contract = \Drupal::entityTypeManager()
    ->getStorage('contracts')
    ->load($entity->get('field_contract')->target_id);
  if (!$contract) {
    return;
  }

  // Only process current year contracts (2026+).
  $contract_year = (int) ($contract->get('field_contract_year')->value ?? 0);
  if ($contract_year < 2026) {
    return;
  }

  // Load property from contract.
  if ($contract->get('field_property')->isEmpty()) {
    return;
  }
  $property_id = (int) $contract->get('field_property')->target_id;

  // Find existing property_spraying_info:weed_spraying for this property.
  $spray_infos = \Drupal::entityTypeManager()
    ->getStorage('property_spraying_info')
    ->loadByProperties([
      'type' => 'weed_spraying',
      'field_property' => $property_id,
    ]);

  if (empty($spray_infos)) {
    // No weed spray record exists — only create if do_you_want = Yes.
    if ($do_you_want !== '1') {
      return;
    }
    $spray_info = \Drupal::entityTypeManager()
      ->getStorage('property_spraying_info')
      ->create([
        'type' => 'weed_spraying',
        'field_property' => $property_id,
        'uid' => \Drupal::currentUser()->id(),
      ]);
  }
  else {
    $spray_info = reset($spray_infos);
  }

  // Get frequency if do_you_want = Yes.
  $frequency_id = NULL;
  if ($do_you_want === '1'
      && $entity->hasField('field_spraying_frequency')
      && !$entity->get('field_spraying_frequency')->isEmpty()) {
    $frequency_id = (int) $entity->get('field_spraying_frequency')->target_id;
  }

  if ($bundle === 'weed_spraying_landscape_beds') {
    if ($do_you_want === '1') {
      $spray_info->set('field_weed_beds_contracted', TRUE);
      $spray_info->set('field_spray_route', TRUE);
      if ($frequency_id) {
        $spray_info->set('field_beds_spraying_frequency', $frequency_id);
      }
    }
    else {
      $spray_info->set('field_weed_beds_contracted', FALSE);
      // Only remove from route if misc is also not contracted.
      $misc_contracted = (bool) $spray_info->get('field_weed_misc_contracted')->value;
      if (!$misc_contracted) {
        $spray_info->set('field_spray_route', FALSE);
      }
    }
  }

  if ($bundle === 'weed_spraying_of_misc_areas') {
    if ($do_you_want === '1') {
      $spray_info->set('field_weed_misc_contracted', TRUE);
      $spray_info->set('field_spray_route', TRUE);
      if ($frequency_id) {
        $spray_info->set('field_misc_spraying_frequency', $frequency_id);
      }
    }
    else {
      $spray_info->set('field_weed_misc_contracted', FALSE);
      // Only remove from route if beds are also not contracted.
      $beds_contracted = (bool) $spray_info->get('field_weed_beds_contracted')->value;
      if (!$beds_contracted) {
        $spray_info->set('field_spray_route', FALSE);
      }
    }
  }

  $spray_info->save();

  \Drupal::logger('contract_residential')->notice(
    'Weed spray route updated for property @pid from contract section @cid (@bundle): do_you_want=@want',
    [
      '@pid' => $property_id,
      '@cid' => $entity->id(),
      '@bundle' => $bundle,
      '@want' => $do_you_want,
    ]
  );
}
