name: 'Contract — Snow Removal'
type: module
description: 'Snow Removal Service Agreement logic: auto contract number (SNOW-{year}-{id}), template-version default, and (later phases) the print/PDF template, QR, and workflow actions.'
package: Contracts
core_version_requirement: ^10 || ^11
dependencies:
  - drupal:eck
