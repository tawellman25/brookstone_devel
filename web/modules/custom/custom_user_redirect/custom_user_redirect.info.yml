name: Custom User Redirect
type: module
description: Redirects to the appropriate profile edit page after user creation based on allowed roles.
core_version_requirement: ^10 || ^11
dependencies:
  - profile:profile