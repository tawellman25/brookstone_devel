name: ECK Bundle Clone
type: module
description: 'Tools to clone ECK bundles (bundle definition + fields + base field overrides). Does NOT clone form/view displays.'
core_version_requirement: ^10 || ^11
package: Custom
dependencies:
  - drupal:eck
  - drupal:field
