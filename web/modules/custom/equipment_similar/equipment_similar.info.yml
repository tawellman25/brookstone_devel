name: 'Equipment — Similar strip'
type: module
description: 'Adds a "Similar Equipment" strip (same field_equipment_type — pic + title) at the bottom of each equipment page, rendering the equipment_similar view.'
core_version_requirement: ^10 || ^11
package: 'Equipment'
dependencies:
  - drupal:eck
  - drupal:views
