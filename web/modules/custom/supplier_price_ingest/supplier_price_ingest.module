<?php

/**
 * @file
 * Supplier Price Ingest — module stub.
 *
 * Phase 3.1 ships the data-model foundation only (entity types, fields,
 * displays, pathauto patterns, permission). No hooks, services, routes,
 * controllers, or forms in this phase.
 *
 * Forthcoming phases:
 *   - 3.2: presave hook (uniqueness invariant on supplier_ingest_config)
 *          + service container (IngestParser, MatchingService, etc.)
 *   - 3.3-3.8: pipeline stages, dashboards, estimator surface
 *   - 3.9: discontinued-material warning on estimate add (uses
 *          field_replaced_by)
 *   - 3.12: SOP authoring
 *
 * See __BOS_AI/Architecture/supplier_pricing_pipeline_phase2.md and
 * supplier_pricing_pipeline_phase3_sequencing.md.
 */
