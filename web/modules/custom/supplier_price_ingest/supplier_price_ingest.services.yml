# Phase 3.1 — empty container. Service definitions land in 3.2 alongside
# the IngestParser, MatchingService, DryRunReporter, and PipelineCommitter
# implementations. Intentionally empty file so the module declares an
# explicit (empty) container rather than implicitly inheriting nothing.

services: {}
