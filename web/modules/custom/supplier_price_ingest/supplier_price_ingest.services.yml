# Phase 3.2 — IngestParser service.
# Matcher, dry-run reporter, commit pipeline services land in 3.3+.

services:
  supplier_price_ingest.parser:
    class: Drupal\supplier_price_ingest\Service\IngestParser
    arguments:
      - '@entity_type.manager'
      - '@file.repository'
      - '@logger.factory'
      - '@current_user'
