{% if delta is even %}
<div class="col-lg-6 col-md-12 col-12 bg-gray">
    <div class="mission dt-section">
        <h3>{{content.field_title.0}}</h3>
        {{content.field_description.0}}
    </div>
</div>
{% else %}
<div class="col-lg-6 col-md-12 col-12 bg-blue">
    <div class="vision dt-section">
        <h3>{{content.field_title.0}}</h3>
        {{content.field_description.0}}
    </div>
</div>
{% endif %}