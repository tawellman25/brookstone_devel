<hr class="section-divider">
<section class="skills-3 dt-section">
    <div class="container">
        <div class="home-3-title">
            {% if content.field_tagline.0 %}
            <span>{{content.field_tagline.0}}</span>
            {% endif %}
            <h2>{{content.field_title.0}}</h2>
        </div>
        <div class="row s-35">
            {{content.field_skill_}}
        </div>
    </div>
</section>