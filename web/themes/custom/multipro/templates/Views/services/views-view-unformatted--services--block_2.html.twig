<section class="services-style-3 dt-section">
    <div class="container">
        <div class="row s-15">
        {% for row in rows %}
            {{row.content}}
        {% endfor %}
        </div>
    </div>
</section>